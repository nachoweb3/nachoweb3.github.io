---
layout: default
title: Página no encontrada
permalink: /404.html
---

<div class="error-page">
    <div class="error-content">
        <div class="error-illustration">
            <span class="error-code">404</span>
            <div class="error-emoji">🤖</div>
        </div>
        <h1>¡Ups! Página no encontrada</h1>
        <p>La página que buscas parece que ha desaparecido en el ciberespacio. Pero no te preocupes, ¡hay mucho contenido interesante esperándote!</p>

        <div class="error-actions">
            <a href="{{ '/' | relative_url }}" class="btn btn-primary btn-large ripple">
                🏠 Volver al Inicio
            </a>
            <a href="{{ '/categorias/ia' | relative_url }}" class="btn btn-secondary">
                🤖 Ver IA
            </a>
            <a href="{{ '/categorias/blockchain' | relative_url }}" class="btn btn-secondary">
                ⛓️ Ver Blockchain
            </a>
        </div>

        <div class="error-search">
            <p>O busca lo que necesitas:</p>
            {% include search-bar.html %}
        </div>

        <div class="error-suggestions">
            <h3>Quizás te interese:</h3>
            <div class="suggestions-grid">
                {% assign recent_posts = site.posts | limit: 3 %}
                {% for post in recent_posts %}
                    <a href="{{ post.url }}" class="suggestion-card">
                        <span class="suggestion-category">{{ post.categories[0] }}</span>
                        <h4>{{ post.title }}</h4>
                    </a>
                {% endfor %}
            </div>
        </div>
    </div>
</div>

<style>
.error-page {
    min-height: 70vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--spacing-xl) var(--spacing-md);
}

.error-content {
    max-width: 600px;
    text-align: center;
}

.error-illustration {
    position: relative;
    margin-bottom: var(--spacing-lg);
}

.error-code {
    font-size: clamp(6rem, 20vw, 10rem);
    font-weight: 900;
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    opacity: 0.3;
}

.error-emoji {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-size: 4rem;
    animation: float 3s ease-in-out infinite;
}

.error-content h1 {
    font-size: clamp(1.5rem, 5vw, 2.5rem);
    margin-bottom: var(--spacing-md);
    color: var(--text-primary);
}

.error-content > p {
    color: var(--text-secondary);
    font-size: 1.1rem;
    margin-bottom: var(--spacing-xl);
    line-height: 1.7;
}

.error-actions {
    display: flex;
    gap: var(--spacing-md);
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: var(--spacing-xl);
}

.error-search {
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    padding: var(--spacing-lg);
    margin-bottom: var(--spacing-xl);
}

.error-search p {
    color: var(--text-secondary);
    margin-bottom: var(--spacing-sm);
}

.error-suggestions h3 {
    margin-bottom: var(--spacing-md);
    color: var(--text-primary);
}

.suggestions-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: var(--spacing-md);
}

.suggestion-card {
    background: var(--bg-primary);
    border: 1px solid var(--border-color);
    border-radius: var(--radius-md);
    padding: var(--spacing-md);
    text-decoration: none;
    transition: all 0.3s ease;
}

.suggestion-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-md);
    border-color: var(--primary-color);
}

.suggestion-category {
    display: block;
    font-size: 0.75rem;
    color: var(--primary-color);
    font-weight: 600;
    margin-bottom: var(--spacing-xs);
}

.suggestion-card h4 {
    font-size: 1rem;
    color: var(--text-primary);
    margin: 0;
}

@media (max-width: 768px) {
    .error-actions {
        flex-direction: column;
    }

    .error-actions .btn {
        width: 100%;
    }
}
</style>
